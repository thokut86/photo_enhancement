# Core image processing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Colorization (OpenCV DNN-based, no extra deps needed beyond opencv-python)
# Model weights downloaded at runtime — see README.md for instructions

# Super-resolution (Real-ESRGAN)
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0
